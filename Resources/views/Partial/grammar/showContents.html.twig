{% if interactionToDisplayed.interaction.question.contents|length != 0 %}

{% if interactionToDisplayed.interaction.question.contents != null %}
    <ul class="list-group">
    {% for content in interactionToDisplayed.interaction.question.contents %}
        <li class="list-group-item">{{ content.media | raw }}</li>
    {% endfor %}
    </ul>
{% endif %}


{% endif %}
